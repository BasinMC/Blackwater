# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: torchmind/circleci-openjdk:9-jdk-browsers
    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "pom.xml" }}
          - v1-dependencies-
      - run: mvn clean install
      - save_cache:
          paths:
            - ~/.m2
          key: v1-dependencies-{{ checksum "pom.xml" }}
      - store_test_results:
          path: pipeline/target/surefire-reports
      - run: |
          mkdir /tmp/artifacts/
          cp pipeline/target/org.basinmc.blackwater-*.jar /tmp/artifacts/
          cp -r target/apidocs/ /tmp/artifacts/
          cp -r target/surefire-reports/ /tmp/artifacts/
      - store_artifacts:
          path: /tmp/artifacts/
          destination: /
